This scripts creates a heatmap graphic using transcript TMM values. To be run in R studio with the list libraries.

Example file type in the script is RSEM but can be used with other file and value types. 
